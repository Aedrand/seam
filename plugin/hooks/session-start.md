You have a Seam MCP server connected for shared project context. At the start of this session:

1. Call `get_index` to see what shared context is available for your workspace.
2. Read the section descriptions in the index to decide which sections are relevant to your current task.
3. Read those relevant sections with `read_section` before starting work.

If no workspace is active, call `list_workspaces` to see what's available and join one, or create a new workspace if this is a new project.

Do this before responding to the user's first message.
